name: Dirty Public Semantic Evaluation

on:
  pull_request:
    paths:
      - samples/dirty_public_corpus_22.json
      - samples/dirty_public_corpus_22_ground_truth.json
      - scripts/evaluate_dirty_public_semantics.py
      - tests/test_dirty_public_semantic_evaluator.py
      - docs/DIRTY_PUBLIC_CORPUS_22_EVALUATION.md
      - .github/workflows/dirty-public-semantic-evaluation.yml
      - app/parsers/**
  push:
    branches:
      - main
    paths:
      - samples/dirty_public_corpus_22.json
      - samples/dirty_public_corpus_22_ground_truth.json
      - scripts/evaluate_dirty_public_semantics.py
      - tests/test_dirty_public_semantic_evaluator.py
      - docs/DIRTY_PUBLIC_CORPUS_22_EVALUATION.md
      - .github/workflows/dirty-public-semantic-evaluation.yml
      - app/parsers/**
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: dirty-public-semantic-${{ github.ref }}
  cancel-in-progress: true

jobs:
  evaluate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.2.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.13"
          cache: pip
      - name: Install local OCR runtime
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y tesseract-ocr tesseract-ocr-eng tesseract-ocr-ita
          tesseract --version
      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt
      - name: Validate evaluator unit behavior
        run: pytest -q tests/test_dirty_public_semantic_evaluator.py tests/test_pdf_semantics.py
      - name: Run granular semantic evaluation
        env:
          THISTINTI_OCR_ENABLED: "true"
          THISTINTI_OCR_LANGUAGES: "ita+eng"
        run: |
          mkdir -p builds
          python scripts/evaluate_dirty_public_semantics.py \
            --manifest samples/dirty_public_corpus_22.json \
            --truth samples/dirty_public_corpus_22_ground_truth.json \
            --report builds/dirty-public-semantic-latest.json \
            --markdown builds/dirty-public-semantic-latest.md
      - name: Print semantic report
        if: always()
        run: |
          if [ -f builds/dirty-public-semantic-latest.md ]; then
            cat builds/dirty-public-semantic-latest.md
          else
            echo "semantic report was not produced"
          fi
      - name: Upload semantic evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dirty-public-semantic-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            builds/dirty-public-semantic-latest.json
            builds/dirty-public-semantic-latest.md
          retention-days: 30
